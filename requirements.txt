openai>=1.50           # Azure OpenAI + OpenAI direct client
anthropic>=0.30        # Anthropic LLM provider
chromadb>=0.5          # Local vector store
sentence-transformers>=3.0   # Local embedding fallback
bertopic>=0.17         # Topic clustering
hdbscan>=0.8           # density-based clustering for BERTopic
umap-learn>=0.5        # 2D layout for the explorer
scikit-learn>=1.3      # CountVectorizer + NearestNeighbors
mcp>=1.0               # MCP server SDK
pyyaml>=6.0            # profile.md frontmatter parsing
requests>=2.30         # remote profile loading + Ollama HTTP client
rank-bm25>=0.2         # lexical leg of hybrid search
langdetect>=1.0        # language of typed messages (WhatsApp, email)
google-api-python-client>=2.100   # optional: sent mail from a Google Workspace mailbox
google-auth>=2.20      # optional: same
pytest>=8.0            # tests
